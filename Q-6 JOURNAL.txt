#include<stdio.h>
#include<conio.h>
#include<string.h>
struct vehicle
{
	 int vid;
	 char name[20];
	 char city[20];
	
};
int main()
{
	struct vehicle v1[5];
	int i;
	for(i=0;i<5;i++)
	{
		printf("\n enter vehicle vid:");
		scanf("%d",&v1[i].vid);
		printf("\n enter vehicle name:");
		scanf("%s",&v1[i].name);
		printf("\n enter vehicle city:");
		scanf("%s",&v1[i].city);
	
	}
	for(i=0;i<5;i++)
	if(strcmp(v1[i].city,"vadodara")==0)
	{
		printf("\n vehicle vid:%d",v1[i].vid);
		printf("\n vehicle name:%s",v1[i].name);
		printf("\n vehicle city:%s",v1[i].city);
	
	}
	getch();
}

